{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "http://openconverters.com/schemas/CAS/CAS.json",
    "title": "CAS",
    "description": "Capacitor Agnostic Structure (CAS). When wrapped with inputs + outputs, forms a valid EAS document.",
    "type": "object",
    "additionalProperties": false,
    "properties": {
        "inputs": {
            "description": "Design requirements and operating points for this capacitor",
            "title": "inputs",
            "$ref": "./inputs.json"
        },
        "capacitor": {
            "description": "The capacitor component data",
            "title": "capacitor",
            "$ref": "./capacitor.json"
        },
        "outputs": {
            "description": "Computed results (ESR losses, thermal, lifetime, etc.)",
            "type": ["object", "array"]
        }
    },
    "required": ["inputs", "capacitor", "outputs"]
}
